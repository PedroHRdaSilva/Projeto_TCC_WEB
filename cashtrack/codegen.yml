overwrite: true
schema: "http://localhost:4000/graphql"
documents: "src/**/*.{ts,tsx}"
generates:
  codegen.yml:
    preset: "client"
    plugins: []
  ./graphql.schema.json:
    plugins:
      - "introspection"
